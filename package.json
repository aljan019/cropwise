{
  "name": "beejrakshak",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,aiml,yield -c cyan,magenta,yellow \"npm run dev:client\" \"npm run dev:aiml\" \"npm run dev:yield\"",
    "dev:client": "cd client && npm run dev",
    "dev:aiml": "cd AIML && python -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "dev:yield": "cd AIML/yield_service && python -m uvicorn app:app --host 0.0.0.0 --port 8002 --reload",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../mobile && npm install && cd ../AIML/yield_service && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}